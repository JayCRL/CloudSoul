# 二进制产物
/cloudshift
/cloudagent
/continuum-server
/continuum-agent
*.exe
/bin/
/dist/

# 环境与密钥
.env
*.env
!.env.example

# 真实会话样本（脱敏后才入 testdata，原始的不提交）
testdata/*.real.jsonl
# 测试用上传样本
testdata/*_test.json
# 临时文件
commit-msg.txt
